# CityTrace Android

Native Android client workspace.

Planned stack:

- Kotlin and Jetpack Compose
- Android location and geofencing APIs
- Google Maps or MapLibre
- Shared CityTrace backend API

Create the Gradle project in this directory. Keep API contracts aligned with
`docs/api-contract.md`.
